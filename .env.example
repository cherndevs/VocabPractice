# Environment Configuration

# Development - uses local SQLite file
NODE_ENV=development

# Optional: Force Turso usage (default: auto-detected)
# USE_TURSO=true

# Production - Turso cloud database
# DATABASE_URL=libsql://your-database-name.turso.io
# TURSO_AUTH_TOKEN=your-auth-token

# The app will automatically:
# - In development: use local SQLite file (local.db)
# - In production: use Turso cloud database (requires DATABASE_URL + TURSO_AUTH_TOKEN)
